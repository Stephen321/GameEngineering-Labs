
public class Producer extends Thread {
	int[] a;	
	
	
	@Override
	public synchronized void start() {
		a = new int[Lab2.n];
	}

	@Override
	public void run() {
		while (p < n) {
		< await (p == c); >
		buf = a[p];
		p = p + 1; } 

	}
	
}
