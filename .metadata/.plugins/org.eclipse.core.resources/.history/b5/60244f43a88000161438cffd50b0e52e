
public class DerinaAllen {

	public static void main(String[] args) {
		Thread t = new Thread(new CheeseFactory()); //fix this
		t.start(); //fix this
		
		PizzaFactory p1 = new PizzaFactory();
		p1.start();
		
		PizzaFactory p2 = new PizzaFactory();
		p2.start();
	}
}
